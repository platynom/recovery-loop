# Raw Razorpay test events

This directory is intentionally empty in source control. Place complete, untouched JSON webhook bodies from Razorpay **test mode** here, one event per file. Do not paste live-mode data, API keys, webhook secrets, customer contact details, or card data into this repository.

The committed files under `data/fixtures/` are clearly labeled synthetic integration fixtures and must never be counted as observed failures.
